#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
	int numero, i;
	
	do {
	printf("Digite o valor do numero: ");
	    scanf("%d", &numero);
	for (i = 1; i <= numero; i++){
		  if (numero % i == 0){
	printf("\n Divisor: %d\n", i);
		}
	}
	} while (numero > 0);
	
}
